<h2>Edit user {{usuario._id}}</h2>
<div class="card">
    <div class="card-body">
        <form action="/admin/users/edit" method="post">
            <input type="hidden" name="id" id="id" value="{{usuario._id}}">
            <div class="mt-3">
                <label for="name">Username</label>
                <input type="text" name="name" id="name" placeholder="Username" class="form-control" value="{{usuario.name}}" required>
            </div>
            <div class="mt-3">
                <label for="email">User email</label>
                <input type="text" name="email" id="email" placeholder="Email" class="form-control" value="{{usuario.email}}" required>
            </div>
            <button type="submit" class="btn btn-success mt-5">Edit user</button>
        </form>
    </div>
</div>